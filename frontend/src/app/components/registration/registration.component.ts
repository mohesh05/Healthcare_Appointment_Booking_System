import { Component } from '@angular/core';import { FormBuilder,ReactiveFormsModule } from '@angular/forms';import { AuthService } from '../../services/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<div class='card'><h3>Register</h3><form [formGroup]='f' (ngSubmit)='submit()'><input formControlName='username'><input formControlName='email'><input type='password' formControlName='password'><select formControlName='role'><option>PATIENT</option><option>DOCTOR</option><option>ADMIN</option></select><button>Register</button></form></div>`})
export class RegistrationComponent{f=this.fb.group({username:'',email:'',password:'',role:'PATIENT'});constructor(private fb:FormBuilder,private auth:AuthService){} submit(){this.auth.register(this.f.value).subscribe();}}
